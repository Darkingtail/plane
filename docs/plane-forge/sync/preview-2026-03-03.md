# preview 上游同步（2026-03-03）

[TOC]

> 同步范围：`9425c66e..a9d688f2`（v1.2.2 之后，尚未发版）
> 提交数量：8
> 变更规模：1053 files changed, +5138 / -8194

---

## 变更内容

### 🔧 工具链重构（影响最大）

| 提交       | 描述             | 影响范围                                                           |
| ---------- | ---------------- | ------------------------------------------------------------------ |
| `41abaffc` | prettier → oxfmt | 全量格式化差异，1000+ 文件，`.prettierrc` 重命名为 `.oxfmtrc.json` |
| `c5542438` | eslint → oxlint  | package.json 脚本替换，新增 `.oxlintrc.json`，移除 eslint 缓存输出 |

> **合并风险：高**。全量格式化变更涉及几乎所有 `.ts/.tsx` 文件，与 feature/dev 的定制代码大概率冲突。建议合并后统一跑一次 `oxfmt` 格式化。

### 🔒 安全/依赖

| 提交       | 描述                                                          | 优先级 |
| ---------- | ------------------------------------------------------------- | ------ |
| `da870a15` | minimatch/rollup 漏洞修复，移除 Sentry 客户端 instrumentation | 🟡 中  |

### ✨ 功能

| 提交       | 描述                                                             | 影响范围           |
| ---------- | ---------------------------------------------------------------- | ------------------ |
| `a9d688f2` | Project Summary external API（`/api/v1/projects/<id>/summary/`） | API 层新增，无冲突 |

### 🐛 修复

| 提交       | 描述                                          | 影响范围                      |
| ---------- | --------------------------------------------- | ----------------------------- |
| `779f5e27` | Wiki heading 布局添加滚动 + i18n 新增翻译 key | 前端 pages 组件 + i18n        |
| `9ee73d57` | 编辑器 merge lists 修复                       | `packages/editor` keymap      |
| `5c9f2a17` | Dockerfile healthcheck 路径改为 `/spaces/`    | `apps/space/Dockerfile.space` |

### 🏗 CI/运维

| 提交       | 描述                                    |
| ---------- | --------------------------------------- |
| `bf52fa22` | Dependabot 更新频率从 weekly 改为 daily |

---

## 与 feature/dev 的冲突分析

feature/dev 仅修改 11 个文件，与上游 1000+ 文件的重叠只有 **1 个**：

| 冲突文件                                       | 说明                                         |
| ---------------------------------------------- | -------------------------------------------- |
| `apps/admin/app/(all)/(dashboard)/ai/form.tsx` | 我们改了 LLM Provider，上游做了 oxfmt 格式化 |

其余 10 个文件（API 后端、docs、types）上游未修改，无冲突风险。
**合并风险：低**。

---

## turbo.json 变更

上游将 `turbo.json` 的 `$schema` 从 `turborepo.com` 改为 `v2-8-12.turborepo.dev`，并将 `globalDependencies` 中 `.prettierrc` + `eslint.config.mjs` 替换为 `.oxfmtrc.json` + `.oxlintrc.json`。lint/format 任务的 `outputs` 改为空数组（不再缓存输出）。

---

## 同步决策

- [ ] 合并到 feature/dev — 冲突量小（仅 1 个文件），可随时合并

---

## 操作步骤

```bash
git checkout feature/dev
git merge preview
# 仅 ai/form.tsx 可能冲突，手动合并我们的 LLM Provider 改动 + 上游格式化即可
```
