# v1.2.2 上游同步分析

[TOC]

> 发版日期：2026-02-23 | **patch** | 我方当前版本：v1.2.1

---

## 变更内容

### 🔒 Security

| 修复项                 | 描述                                   | 优先级 |
| ---------------------- | -------------------------------------- | ------ |
| API Token 速率限制绕过 | 服务端校验缺失，任意用户可修改速率限制 | 🔴 高  |
| SSRF（work item 链接） | URL 未严格校验，可发起内网请求         | 🔴 高  |
| 成员信息泄露           | 公开端点未鉴权，可获取成员详情         | 🔴 高  |
| IDOR（资产/附件端点）  | 未授权可访问他人资源                   | 🔴 高  |
| Django → 4.2.28        | 依赖升级                               | 🟡 中  |
| cryptography → 46.0.5  | 依赖升级                               | 🟡 中  |

---

## 与 feature/dev 的差异

- 全部为安全修复，无功能变更
- 与我们的定制改动（LLM Provider、CSRF local.py）**无冲突风险**

---

## 同步决策

- [x] **必须合并** — 4 个安全漏洞，影响生产环境
- [ ] 已合并到 feature/dev

---

## 操作步骤

```bash
git checkout preview
git fetch upstream
git merge --ff-only upstream/preview
git push origin preview

git checkout feature/dev
git merge preview
# 预期无冲突，安全补丁集中在 API 层
```
